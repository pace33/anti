# 에이두 국어 구조도

최종 갱신: 2026-07-10
운영 주소: `https://aiedue.ddns.net/korean/`

## 1. 애플리케이션 구조

에이두 국어는 별도의 프레임워크 빌드 없이 Caddy에서 제공하는 정적 웹앱이다.
화면 마크업, 스타일, 기능 로직을 다음처럼 분리한다.

```text
anti/
├─ index.html                 화면 마크업과 모달
├─ style.css                 공통 디자인 시스템
├─ app.css                   활동별 컴포넌트와 접근성 스타일
├─ app.js                    인증, 화면 전환, 학습 활동 로직
├─ firebase-config.js        에이두 공용 Firebase 설정
├─ drawing.html              그리기 바로가기
├─ hangul.html               한글 바로가기
├─ dictation.html            받아쓰기 바로가기
├─ literacy.html             문해력 바로가기
├─ assets                    이미지·음원 파일(현재는 루트에 위치)
├─ tools/
│  └─ verify-korean-sketchbook.ps1
└─ .github/workflows/
   └─ deploy-korean.yml
```

`drawing.html`, `hangul.html`, `dictation.html`, `literacy.html`은 독립 앱이 아니다.
각 파일은 `index.html?activity=...`로 이동시키는 짧은 바로가기다.

## 2. 화면 흐름

```text
시작 화면
└─ 로그인
   ├─ 학생: 숫자 로그인 번호
   └─ 교사: 이메일 또는 Google 로그인
      ↓
대시보드
├─ 1단계 그리기
├─ 2단계 한글 해득
├─ 3단계 받아쓰기
└─ 4단계 문해력
```

화면 전환은 `app.js`가 각 섹션의 `hidden` 클래스를 전환하는 방식으로 처리한다.
현재 로그인 번호, 테스트 로그인, 역할 생성 방식은 학교 현장 편의성을 위해 기존 방식을 유지한다.

## 3. 주요 기능

### 그리기

- 자유 그리기와 도형 미션
- 정확도 판정과 포인트 보상
- 개인 포트폴리오와 친구 작품 갤러리
- 작품과 진행 상태는 Firebase Firestore에 저장

### 한글 해득

- 배움 시작과 34개 세부 배움
- 글자·단어·문장 따라쓰기
- 그림-낱말 연결, 듣고 찾기, 소리 게임
- 활동별 정답률·오류 유형·재시도 기록

### 받아쓰기

- 단어·문장 은행
- 태블릿 카메라 또는 파일 선택
- Tesseract OCR과 `/korean-ai/generate` 이미지 분석
- 오답 은행과 완료 은행

### 문해력

- 쉬움·보통·어려움·전문가 난이도
- 객관식·단답형·서술형
- 오늘의 미션, 한계 돌파, 오답 누적

### 교사 기능

- 학급 학생 목록
- 학생별 단계 설정
- 학습 리포트
- 상점 물품 제작과 학생별 배부

## 4. 데이터 구조

Firebase Auth와 Firestore가 인증과 영구 저장의 기준이다.

```text
Firebase Auth
└─ 로그인 세션

Firestore
├─ users/{uid}
├─ classes/{teacherUid}
├─ korean_attempts/{attemptId}
├─ aiedueKoreanDrawings/{drawingId}
├─ shopItems/{itemId}
├─ purchaseLog/{logId}
└─ sharedLiteracyWrongBank/{questionId}
```

브라우저에는 영구 학습 데이터를 저장하지 않는다. 화면 갱신에 필요한 값만 현재 세션의
메모리에 두고, 그림·학습 시도·진행도·받아쓰기·문해력 기록은 Firestore에 저장한다.
과거 `/korean-db`로 그림을 이중 저장하던 미러 경로와 사용되지 않던
`school-firestore-adapter.js`는 제거했다.

## 5. 외부 서비스

- Firebase JS SDK: 인증과 Firestore
- `/korean-ai/generate`: OCR 보완과 사진 분석
- `/.netlify/functions/generatePlan`: 맞춤법·문해력 문제 생성
- Tesseract.js: 카메라 사진 OCR, 필요한 시점에만 로드
- Web Speech API / Google TTS 보조 경로: 한국어 음성

학교망에서 외부 CDN이 차단될 가능성이 있으므로 네트워크 오류가 발생하면
Firebase SDK, Google Fonts, Tailwind CDN, Tesseract CDN 접근 여부를 먼저 확인한다.

## 6. 배포

`main` 브랜치에 병합되면 Windows 자체 실행기에서 다음 순서로 배포한다.

1. 저장소 체크아웃
2. 정적 구조·문법·필수 기능 마커 검사
3. 허용된 운영 파일만 Caddy 정적 폴더로 복사
4. Caddy 설정 검증과 다시 불러오기
5. 공개 `/korean` 주소 스모크 테스트

작업 자료, 개인 자료, 생성 중간 파일은 저장소 루트에 두지 않는다.
필요한 자료는 별도 비공개 저장소나 로컬 작업 폴더에서 관리한다.

## 7. 수정 원칙

- 로그인과 역할 흐름은 별도 요청 없이 변경하지 않는다.
- 사용자·AI·Firestore 문자열을 HTML로 출력할 때 반드시 이스케이프한다.
- 새 클릭 요소는 가능한 한 실제 `button`을 사용한다.
- `index.html`, `app.js`, `app.css`를 함께 검사한다.
- 기능 수정은 별도 브랜치와 PR에서 확인한 뒤 `main`에 병합한다.
